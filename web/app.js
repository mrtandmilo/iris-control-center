const API='/iris-control-center/api';let catalogue=[];let selected=null;
const el=id=>document.getElementById(id);const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
async function load(){el('services').innerHTML='<p class="muted">Discovering services…</p>';try{const r=await fetch(`${API}/services`);if(!r.ok)throw Error(`${r.status} ${r.statusText}`);const data=await r.json();catalogue=data.services||[];el('count').textContent=catalogue.length;renderList()}catch(e){el('services').innerHTML=`<p class="muted">Unable to load catalogue: ${esc(e.message)}</p>`}}
function renderList(){const q=el('filter').value.toLowerCase();const rows=catalogue.filter(x=>`${x.name} ${x.webApplication} ${x.namespace}`.toLowerCase().includes(q));el('services').innerHTML=rows.length?rows.map((s,i)=>`<div class="service ${selected===s.name?'active':''}" data-name="${esc(s.name)}"><strong>${esc(s.name||s.webApplication||'Unnamed service')}</strong><small>${esc(s.webApplication||'No web application')}</small></div>`).join(''):'<p class="muted">No matching services.</p>';document.querySelectorAll('.service').forEach(x=>x.onclick=()=>selectService(x.dataset.name))}
async function selectService(name){selected=name;renderList();const s=catalogue.find(x=>x.name===name);el('detail').innerHTML=`<h2>${esc(s.name)}</h2><div class="meta"><div class="card"><label>Web application</label>${esc(s.webApplication||'—')}</div><div class="card"><label>Namespace</label>${esc(s.namespace||'—')}</div><div class="card"><label>Dispatch class</label>${esc(s.dispatchClass||'—')}</div><div class="card"><label>OpenAPI</label>${esc(s.swagger||'Not advertised')}</div></div><h3>Endpoints</h3><div id="endpoints"><p class="muted">Loading API definition…</p></div>`;if(!s.swagger){el('endpoints').innerHTML='<p class="muted">This service does not advertise an OpenAPI definition.</p>';return}try{const r=await fetch(`${API}/openapi/${encodeURIComponent(name)}`);if(!r.ok)throw Error(`${r.status} ${r.statusText}`);const spec=await r.json();renderSpec(spec)}catch(e){el('endpoints').innerHTML=`<p class="muted">Could not load OpenAPI definition: ${esc(e.message)}</p>`}}
function renderSpec(spec){const rows=[];for(const [path,ops] of Object.entries(spec.paths||{}))for(const [method,op] of Object.entries(ops||{})){if(!['get','post','put','patch','delete','head','options'].includes(method))continue;rows.push(`<div class="endpoint"><span class="method">${method.toUpperCase()}</span><code>${esc(path)}</code><p class="muted">${esc(op.summary||op.description||'')}</p></div>`)}el('endpoints').innerHTML=rows.join('')||'<p class="muted">No paths were found in this API definition.</p>'}
el('filter').oninput=renderList;el('refresh').onclick=load;load();
